import styles from './SortMenu.module.scss';

const SortMenu = () => {
    return (
        <section className={styles['sort-menu']}>
            Sort Menu
        </section>
    )
};

export default SortMenu;
